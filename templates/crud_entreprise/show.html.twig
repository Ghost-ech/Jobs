{% extends 'base.html.twig' %}

{% block title %}Entreprise{% endblock %}

{% block body %}
    <h1>Entreprise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entreprise.id }}</td>
            </tr>
            <tr>
                <th>Info_supp</th>
                <td>{{ entreprise.infoSupp }}</td>
            </tr>
            <tr>
                <th>Nom_entreprise</th>
                <td>{{ entreprise.nomEntreprise }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ entreprise.localisation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_entreprise_index') }}">back to list</a>

    <a href="{{ path('app_crud_entreprise_edit', {'id': entreprise.id}) }}">edit</a>

    {{ include('crud_entreprise/_delete_form.html.twig') }}
{% endblock %}
